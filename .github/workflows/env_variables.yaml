name: GitHub Env. Variables
on: [push]
jobs:
  Test-Env-Variables:
    runs-on: ubuntu-latest
    env:
      GLOB_ENV_VAR: Global Var
    steps:
      - name: "Execute if it's GLOB_ENV_VAR"
        if: ${{ env.GLOB_ENV_VAR == 'Test' }}
        run: echo "Event=${{ github.event_name }} GLOB_ENV_VAR=$env.GLOB_ENV_VAR LOCAL_ENV_VAR=$LOCAL_ENV_VAR"
        env:
          LOCAL_ENV_VAR: Local Var
          
